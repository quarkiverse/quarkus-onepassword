demo.secret=${op::op://Development/Demo/credential}
# A deliberately nonexistent CLI proves packaging never performs a lookup.
# Override with -Donepassword.cli-path=op when running this example.
#onepassword.cli-path=op-must-not-be-invoked-during-build
